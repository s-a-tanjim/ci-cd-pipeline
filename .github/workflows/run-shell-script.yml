name: Run Shell Script

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run shell script
        run: scripts/git-hash-parse.sh
        shell: bash